import 'package:flutter/material.dart';

class TtospayFragment extends StatelessWidget {
  const TtospayFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}

